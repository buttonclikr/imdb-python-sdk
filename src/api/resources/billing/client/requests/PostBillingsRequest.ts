/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as ButtonclikrApi from "../../../../index";

/**
 * @example
 *     {
 *         login: {
 *             "key": "value"
 *         },
 *         entity: {
 *             "key": "value"
 *         },
 *         forentity: {
 *             "key": "value"
 *         },
 *         org: {
 *             "key": "value"
 *         },
 *         division: {
 *             "key": "value"
 *         },
 *         partition: {
 *             "key": "value"
 *         },
 *         description: {
 *             "key": "value"
 *         },
 *         schedule: ButtonclikrApi.PostBillingsRequestSchedule.Days,
 *         scheduleFactor: {
 *             "key": "value"
 *         },
 *         start: {
 *             "key": "value"
 *         },
 *         finish: {
 *             "key": "value"
 *         },
 *         collectionFactor: ButtonclikrApi.PostBillingsRequestCollectionFactor.Days,
 *         collectionOffset: {
 *             "key": "value"
 *         },
 *         collectionIncludeCurrent: ButtonclikrApi.PostBillingsRequestCollectionIncludeCurrent.Zero,
 *         currency: ButtonclikrApi.PostBillingsRequestCurrency.Aed,
 *         inactive: ButtonclikrApi.PostBillingsRequestInactive.Zero,
 *         frozen: ButtonclikrApi.PostBillingsRequestFrozen.Zero
 *     }
 */
export interface PostBillingsRequest {
    login?: unknown;
    entity?: unknown;
    forentity?: unknown;
    org?: unknown;
    division?: unknown;
    partition?: unknown;
    description?: unknown;
    /** The schedule that determines when this Billing is triggered to start collecting data, which can be either Daily, Weekly, Monthly, or Annual. */
    schedule: ButtonclikrApi.PostBillingsRequestSchedule;
    scheduleFactor?: unknown;
    start?: unknown;
    finish?: unknown;
    /**
     * A multiplier that you can use to adjust the set of data to be used in the collection calculation.
     *
     * <details>
     * <summary>Valid Values</summary>
     *
     * - `days` - **Daily Calculation.**
     *
     * - `weeks` - **Weekly Calculation.**
     *
     * - `months` - **Monthly Calculation.**
     *
     * - `years` - **Annual Calculation.**
     *
     * </details>
     *
     */
    collectionFactor: ButtonclikrApi.PostBillingsRequestCollectionFactor;
    collectionOffset?: unknown;
    /**
     * Whether this billing collection is calculated based on the entity's current amount.
     *
     * <details>
     * <summary>Valid Values</summary>
     *
     * - `0` - **Do not include current balance.**
     *
     * - `1` - **Include current balance.**
     * </details>
     *
     */
    collectionIncludeCurrent: ButtonclikrApi.PostBillingsRequestCollectionIncludeCurrent;
    /** The currency of the amount in this assessments resource. */
    currency: ButtonclikrApi.PostBillingsRequestCurrency;
    /** Whether this resource is marked as inactive. Valid Values: `0` - Active, `1` - Inactive. */
    inactive: ButtonclikrApi.PostBillingsRequestInactive;
    /** Whether this resource is marked as frozen. Valid Values: `0` - Not Frozen, `1` - Frozen. */
    frozen: ButtonclikrApi.PostBillingsRequestFrozen;
}
