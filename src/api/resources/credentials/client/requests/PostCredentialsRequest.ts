/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as ButtonclikrApi from "../../../../index";

/**
 * @example
 *     {
 *         entity: {
 *             "key": "value"
 *         },
 *         name: {
 *             "key": "value"
 *         },
 *         description: {
 *             "key": "value"
 *         },
 *         username: {
 *             "key": "value"
 *         },
 *         password: {
 *             "key": "value"
 *         },
 *         connectUsername: {
 *             "key": "value"
 *         },
 *         connectPassword: {
 *             "key": "value"
 *         },
 *         integration: ButtonclikrApi.PostCredentialsRequestIntegration.Apple,
 *         type: ButtonclikrApi.PostCredentialsRequestType.Transaction,
 *         secret: {
 *             "key": "value"
 *         },
 *         inactive: ButtonclikrApi.PostCredentialsRequestInactive.Zero,
 *         frozen: ButtonclikrApi.PostCredentialsRequestFrozen.Zero
 *     }
 */
export interface PostCredentialsRequest {
    entity?: unknown;
    name?: unknown;
    description?: unknown;
    username?: unknown;
    password?: unknown;
    connectUsername?: unknown;
    connectPassword?: unknown;
    /**
     * The payment platform integration using credential logins.
     *
     * <details>
     * <summary>Valid Values</summary>
     *
     * - APPLE - **Apple API**
     *
     * - ELAVON - **Elavon API**
     *
     * - FEDACH - **FedACH Account Integration**
     *
     * - FIRSTDATA - **FirstData / Clover Integration**
     *
     * - NEUTRINO - **Neutrino eCommerce Integration**
     *
     * - OFAC - **Office of Foreign Assets Control Sanctions**
     *
     * - bigbux - **bigbux API**
     *
     * - PLAID - **Plaid Account Integration**
     *
     * - SIFT - **Sift Fraud Management**
     *
     * - SOCURE - **Socure ID Verification**
     *
     * - SOUNDPAYMENTS - **Sound Payments POS**
     *
     * - TDBANK - **TD Bank Platform**
     *
     * - VANTIV - **WorldPay / Vantiv eComm Platform**
     *
     * - VCORE - **WorldPay / Vantiv Core Platform**
     *
     * - WEBSHIELD - **Web Shield Merchant Monitoring**
     *
     * - WELLSACH - ** Wells Fargo Merchant Services ACH Processor**
     *
     * - WELLSFARGO - **Wells Fargo Merchant Services Payment Processor**
     *
     * - WFSINGLE - **Wells Fargo Single Payment Processors**
     *
     * </details>
     *
     */
    integration: ButtonclikrApi.PostCredentialsRequestIntegration;
    /** The type of Transaction. Valid Values: `1` - Credit Card Only: Sale Transaction. Processes a sale and charges the customer, `2` - Credit Card Only: Auth Transaction. Authorizes and holds the requested total on the credit card, `3` - Credit Card Only: Capture Transaction. Finalizes a prior Auth Transaction and charges the customer, `4` - Credit Card Only: Reverse Authorization. Reverses a prior Auth or Sale Transaction and releases the credit hold, `5` - Credit Card Only: Refund Transaction. Refunds a prior Capture or Sale Transaction (total may be specified for a partial refund), `7` - Echeck Only: Echeck Sale Transaction. Sale Transaction for ECheck payment, `8` - Echeck Only: ECheck Refund Transaction. Refund Transaction for prior ECheck Sale Transaction, `11` - Echeck Only: Echeck Redeposit Transaction. Attempt to redeposit a prior failed eCheck Sale Transaction, `12` - Echeck Only: Echeck Account Verification Transaction. Attempt to verify eCheck payment details. */
    type: ButtonclikrApi.PostCredentialsRequestType;
    secret?: unknown;
    /** Whether this resource is marked as inactive. Valid Values: `0` - Active, `1` - Inactive. */
    inactive: ButtonclikrApi.PostCredentialsRequestInactive;
    /** Whether this resource is marked as frozen. Valid Values: `0` - Not Frozen, `1` - Frozen. */
    frozen: ButtonclikrApi.PostCredentialsRequestFrozen;
}
