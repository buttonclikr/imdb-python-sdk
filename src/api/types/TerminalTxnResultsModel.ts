/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as ButtonclikrApi from "../index";

export interface TerminalTxnResultsModel {
    id?: ButtonclikrApi.Id;
    created?: ButtonclikrApi.Created;
    modified?: ButtonclikrApi.Modified;
    creator?: ButtonclikrApi.Creator;
    modifier?: ButtonclikrApi.Modifier;
    /** The identifier of the TerminalTxn associated with this terminalTxnResults resource. */
    terminalTxn?: string;
    /**
     * The type of this terminalTxnResult.
     *
     * <details>
     * <summary>Valid Values</summary>
     *
     * - `1` - **General Result.**
     * - `2` - **Fraud Prevention Alert.**
     * - `3` - **Processing Error.**
     * - `4` - **CVV Matching Alert.**
     * - `5` - **AVS Check Alert.**
     * - `6` - **AAVS Check Alert.**
     * - `7` - **Network Alert.**
     * </details>
     */
    type?: ButtonclikrApi.TerminalTxnResultsModelType;
    /** A message that accompanies and describes this Transaction result. */
    message?: string;
    /**
     * The result code that is associated with this terminal TxnResult. \
     *
     *  <details>
     * <summary>Valid Values</summary>
     *
     * - `0` - **Transaction approved.**
     *
     * - `1` - **Partially approved. The processor has only approved a portion of the total transaction amount.**
     *
     * - `2` - **Declined. The processor has declined the Transaction.**
     *
     * - `3` - **The supplied CVV or AVS code matches.**
     *
     * - `4` - **The supplied CVV or AVS code does not match.**
     *
     * - `5` - **The ZIP code in the Transaction data does not match the customer details held by the card issuer.**
     *
     * - `6` - **The address in the Transaction data does not match the customer details held by the card issuer.**
     *
     * - `7` - **The name in the Transaction data does not match the customer details held by the card issuer.**
     *
     * - `8` - **The name and phone number in the Transaction data do not match the details held by the card issuer.**
     *
     * - `9` - **The name and email address in the Transaction data do not match the customer details held by the card issuer.**
     *
     * - `10` - **The phone number in the Transaction data does not match the customer details held by the card issuer.**
     *
     * - `11` - **The phone number and email address in the Transaction data do not match the customer details held by the card issuer.**
     *
     * - `12` - **The email address in the Transaction data does not match the customer details held by the card issuer.**
     *
     * - `13` - **The customer name could not be found in the Transaction data.**
     *
     * - `14` - **The customer name and phone number were not found in the Transaction data.**
     *
     * - `15` - **The customer name and email address were not found in the Transaction data.**
     *
     * - `16` - **The customer phone number was not found in the Transaction data.**
     *
     * - `17` - **The customer phone number and email address were not found in the Transaction data.**
     *
     * - `18` - **The customer email address was not found in the Transaction data.**
     *
     * - `19` - **Information about the customer was not found in the Transaction data.**
     *
     * - `20` - **Non-sufficient funds. The customer did not have sufficient credit or balance to cover the Transaction.**
     *
     * - `21` - **The account in the Transaction data is not valid.**
     *
     * - `22` - **The account is unauthorized.**
     *
     * - `23` - **General error.**
     *
     * - `24` - **The ZIP code in the Transaction data was not verified.**
     *
     * - `25` - **The ZIP code and the Address in the Transaction data were not verified.**
     *
     * - `26` - **The Address in the Transaction data was not verified.**
     *
     * - `27` - **Txn did not fail but was not captured.**
     *
     * - `28` - **3DS authentication passed.**
     *
     * - `29` - **3DS authentication is invalid.**
     *
     * - `30` - **3DS authentication failed.**
     *
     * - `31` - **3DS authentication was not validated.**
     *
     * - `32` - **3DS authentication passed but liability was not shifted.**
     *
     * - `33` - **Transaction processing timed out**
     *
     * - `34` - **Transaction cancelled**
     * </details>
     */
    code?: ButtonclikrApi.TerminalTxnResultsModelCode;
    /** Exact message code as received from the activation integration or from the processor for the resulting transaction. */
    originalCode?: string;
    /**
     * Transaction result bank platform code. \
     *
     * <details>
     * <summary>Valid Values</summary>
     *
     * - `accepted_but_not_yet_processed` - **Fiserv (aka First Data) only, code 385, accepted but not yet processed.**
     *
     * - `account_blocked` - **Fiserv (aka First Data) only, code 224, account blocked - possible.**
     *
     * - `account_is_purged` - **Fiserver (aka First Data) only, code 378, account is purged - the account record was purged from the database.**
     *
     * - `account_locked` - **Fiserv (aka First Data) only, code 332, account locked**
     *
     * - `account_on_hold_or_not_active` - **Fiserv (aka First Data) only, code 370, Account on hold or subscriber not active**
     *
     * - `account_reloads_exceeded` - **Fiserv (aka First Data) only, code 322, Exceeded account reloads. The clerk/user/location was only permitted to reload some number of accounts. That number was exceeded. (See your Business Manager in order to extend this limit.)**
     *
     * - `account_row_locked` - **Fiserv (aka First Data) only, code 384, account row locked.**
     *
     * - `account_status_is_void_lock` - **Fiserv (aka First Data) only, code 376, account status is void lock.**
     *
     * - `ach_payment_not_accepted` - **Unused. ACH payment is not accepted.**
     *
     * - `acquirer_channel_unavailable` - **Fiserv (aka First Data) only, code 963, acquirer channel unavailable.**
     *
     * - `activation_failed` - **Fiserv (aka First Data) only, code 151, activation failed.**
     *
     * - `additive_limit_exceeded` - **Fiserv (aka First Data) only, code 568, over additive limit.**
     *
     * - `zip_match_address_mismatch` - **TBD**
     *
     * - `zip_match_address_not_verified` - **TBD**
     *
     * - `already_redeemed` - **Fiserv (aka First Data) only, code 568, over additive limit.**
     *
     * - `already_reversed` - **Fiserv (aka First Data) only, code 334, already reversed.**
     *
     * - `amount_limit_exceeded` - **Fiserv (aka First Data) only, codes 587 and 771, transaction amount is above the limit allowed.**
     *
     * - `amount_too_large` - **WorldPay (aka Vantiv) Core only, code 066, transaction amount too large.**
     *
     * - `approve_with_id` - **Transaction approved with identification.**
     *
     * - `approved` - **Transaction approved.**
     *
     * - `approved_emv_key_load` - **Fiserv (aka First Data) only, codes 701 and 703, EMV key load approved.**
     *
     * - `approved_with_no_cash` - **Transaction purchase amount approved but not cash.**
     *
     * - `approved_with_overdraft` - **WorldPay (aka Vantiv) Core only, code 20, transaction approved with overdraft.**
     *
     * - `authorization_id_needed` - **Fiserv (aka First Data) only, code 806, authorization ID needed.**
     *
     * - `bad_account` - **Electronic check transaction returned due to incorrect account details.**
     *
     * - `bad_checksum` - **Fiserv (aka First Data) only, code 330, bad checksum. The checksum provided is incorrect.**
     *
     * - `bad_close` - **Bad close on gift card.**
     *
     * - `bad_mag_stripe` - **Fiserv (aka First Data) only, code 315, bad mag stripe. The mag stripe could not be parsed for account information.**
     *
     * - `bad_post_date` - **Fiserv (aka First Data) only, code 375, bad Post Date. The Post Date is not a valid date.**
     *
     * - `bad_repay_date` - **Fiserv (aka First Data) only, code 329, bad Repay Date.**
     *
     * - `balance_not_available` - **Fiserv (aka First Data) only, code 331, balance not available (denial). Due to an internal First Data Prepaid Closed Loop issue, information from this account could not be retrieved.**
     *
     * - `bill_to_ship_to_mismatch` - **Fiserv (aka First Data) only, code 225, bill-to address does not match ship-to.**
     *
     * - `bin_blocked` - **Fiserv (aka First Data) only, code 238, BIN (Bank Issuer Number) blocked.**
     *
     * - `bulk_activation_error` - **Fiserv (aka First Data) only, codes 380 and 381, bulk activation error.**
     *
     * - `bulk_activation_package_amount_error` - **Fiserv (aka First Data) only, code 382, bulk activation package amount error.**
     *
     * - `call_acquirer` - **Fiserv (aka First Data) only, code 105, call acquirer’s security department.**
     *
     * - `call_for_authorization` - **Fiserv (aka First Data) only, codes 107, 727, M06 and V06, call for authorization.**
     *
     * - `call_issuer` - **Call issuing bank.**
     *
     * - `call_telecheck` - **Fiserv (aka First Data) only, code V85, call TeleCheck.**
     *
     * - `cannot_find_network` - **Fiserv (aka First Data) only, code V73, cannot find institution or network.**
     *
     * - `card_already_active` - **Gift card already active.**
     *
     * - `card_already_closed` - **Gift card already closed.**
     *
     * - `card_not_active` - **Gift card not active.**
     *
     * - `card_not_supported` - **Card not supported.**
     *
     * - `card_velocity_amount_exceeded` - **Fiserv (aka First Data) only, code 810, over card velocity amount.**
     *
     * - `card_velocity_count_exceeded` - **Fiserv (aka First Data) only, code 809, over card velocity count.**
     *
     * - `card_velocity_exceeded` - **Card velocity (count and/or amount) exceeded.**
     *
     * - `cashback_greater_than_transaction_amount` - **Fiserv (aka First Data) only, code 522, Cash Back greater than total Transaction amount.**
     *
     * - `cash_limit_exceeded` - **Fiserv (aka First Data) only, code 564, over cash limit.**
     *
     * - `center_closed` - **Fiserv (aka First Data) only, codes M21, M27 and V27, center closed.**
     *
     * - `checker/manager_not_found` - **Fiserv (aka First Data) only, code 572, checker/manager not found.**
     *
     * - `chip_failure` - **Chip failure.**
     *
     * - `close_unavailable` - **Fiserv (aka First Data) only, code 603, close unavailable.**
     *
     * - `close_not_valid` - **Fiserv (aka First Data) only, code 604, close not valid.**
     *
     * - `closed_account` - **Fiserv (aka First Data) only, codes 229, 232 and 302, account closed.**
     *
     * - `count_exceeds_limit` - **Fiserv (aka First Data) only, codes 782 and V63, count exceeds limit.**
     *
     * - `counterfeit_card` - **Fiserv (aka First Data) only, code 129, suspected counterfeit card.**
     *
     * - `counter_offer_to_supply_personal_guaranty` - **Fiserv (aka First Data) only, code 254, counter offer to supply personal guaranty.**
     *
     * - `cryptogram_decryption_error` - **Fiserv (aka First Data) only, code 530, the authorizing network has a problem decrypting the cryptogram in the request.**
     *
     * - `crypto_box_is_offline` - **Fiserv (aka First Data) only, code 523, crypto box is offline.**
     *
     * - `currency_conversion_complete` - **Currency conversion completed, no authorization performed.**
     *
     * - `currency_conversion_error` - **Fiserv (aka First Data) only, code 357, currency conversion error.**
     *
     * - `currency_not_supported` - **Currency not supported.**
     *
     * - `customer_cancellation` - **Customer cancellation.**
     *
     * - `customer_opt_out` - **Fiserv (aka First Data) only, codes 242 and 243 (the latter is incorrect as code 243 is related to institution acceptance of ACH payments), customer opt-out.**
     *
     * - `cut_time_close_to_1400` - **Fiserv (aka First Data) only, code 571, cut time too close to 1400.**
     *
     * - `cvv_declined` - **CVV value provided declined.**
     *
     * - `cvv_failed` - **Fiserv (aka First Data) only, codes 954, 958 and 959, CVV failed.**
     *
     * - `daily_limit_exceeded` - **Daily transaction limit exceeded.**
     *
     * - `date_of_birth_error` - **Fiserv (aka First Data) only, code 501, date of birth error for check processing.**
     *
     * - `debit_gateway_unavailable` - **Fiserv (aka First Data) only, codes 524 and 525, debit gateway unreachable or cannot reach debit issuer.**
     *
     * - `declined` - **Transaction declined.**
     *
     * - `declined_due_to_stand_in_rules` - **Fiserv (aka First Data) only, code 247, declined due to stand-in rules.**
     *
     * - `deconverted_bin` - **Deconverted BIN (Bank Issuer Number) for gift card.**
     *
     * - `do_not_honor` - **Do not honor transaction.**
     *
     * - `do_not_try_again` - **Fiserv (aka First Data) only, codes 504, 509 and 520, do not try authorizing again.**
     *
     * - `do_not_try_again_subsequent_may_continue` - **Fiserv (aka First Data) only, code 790, not approved. Used only in Visa bill/recurring payment. Merchant must not resubmit same transaction but may continue billing process in subsequent billing period.**
     *
     * - `drivers_license_or_id_required` - **Fiserv (aka First Data) only, code 723, driver’s license or ID required.**
     *
     * - `duplicate_capture` - **Fiserv (aka First Data) only, code 776, duplicate completion.**
     *
     * - `duplicate_return` - **Fiserv (aka First Data) only, code 772, duplicate return.**
     *
     * - `duplicate_reversal` - **Fiserv (aka First Data) only, code 774, duplicate reversal.**
     *
     * - `duplicate_transaction` - **Duplicate transaction.**
     *
     * - `edit_honor` - **Fiserv (aka First Data) only, code 540, edit honor.**
     *
     * - `email_no_match` - **AAVS verification partially successful but email provided did not match.**
     *
     * - `email_unavailable` - **AAVS verification partially successful but email matching was unavailable.**
     *
     * - `emv_key_download_error` - **Fiserv (aka First Data) only, code 702, EMV key download error.**
     *
     * - `enter_lesser_amount` - **Fiserv (aka First Data) only, code 521, enter lesser amount.**
     *
     * - `error_processing_pin_block` - **Fiserv (aka First Data) only, code 542, DUKPT: An error while processing the PIN block that is not related to the point-of-sale equipment. Contact the Help Desk for assistance.**
     *
     * - `exceeds_global_amount_for_period_pay_inside` - **Fiserv (aka First Data) only, code 838, exceeds global amount for period - pay inside.**
     *
     * - `exceeds_global_count_for_period_pay_inside` - **Fiserv (aka First Data) only, code 837, exceeds global count for period - pay inside.**
     *
     * - `exceeds_global_limits_for_period_pay_inside` - **Fiserv (aka First Data) only, code 839, exceeds global limits for period - pay inside.**
     *
     * - `exceeds_limit` - **Transaction amount exceeds limit.**
     *
     * - `exceeds_merchant_amount_for_period_pay_inside` - **Fiserv (aka First Data) only, code 829, exceeds merchant amount for period - pay inside.**
     *
     * - `exceeds_merchant_count_for_period_pay_inside` - **Fiserv (aka First Data) only, code 828, exceeds merchant count for period - pay inside.**
     *
     * - `exceeds_merchant_limits_for_period_pay_inside` - **Fiserv (aka First Data) only, code 830, exceeds merchant limits for period - pay inside.**
     *
     * - `exceeds_state_amount_for_period_pay_inside` - **Fiserv (aka First Data) only, code 835, exceeds state amount for period - pay inside.**
     *
     * - `exceeds_state_count_for_period_pay_inside` - **Fiserv (aka First Data) only, code 834, exceeds state count for period - pay inside.**
     *
     * - `exceeds_state_limits_for_period_pay_inside` - **Fiserv (aka First Data) only, code 836, exceeds state limits for period - pay inside.**
     *
     * - `exceeds_transaction_count_or_amount_limit_in_24_hours` - **Fiserv (aka First Data) only, codes 578, 581 and 584, The Visa OCT / MasterCard MoneySend activity has exceeded preset transaction count or amount limit within a rolling 24-hour period for given merchant.**
     *
     * - `exceeds_transaction_count_or_amount_limit_in_7_days` - **Fiserv (aka First Data) only, codes 579, 582 and 585, The Visa OCT / MasterCard MoneySend activity has exceeded preset transaction count or amount limit within a rolling 7-day period for given merchant.**
     *
     * - `exceeds_transaction_count_or_amount_limit_in_30_days` - **Fiserv (aka First Data) only, codes 580, 583 and 586, The Visa OCT / MasterCard MoneySend activity has exceeded preset transaction count or amount limit within a rolling 30-day period for given merchant.**
     *
     * - `exceeds_zip_code_amount_for_period_pay_inside` - **Fiserv (aka First Data) only, code 832, exceeds zip code amount for period - pay inside.**
     *
     * - `exceeds_zip_code_count_for_period_pay_inside` - **Fiserv (aka First Data) only, code 831, exceeds zip code count for period - pay inside.**
     *
     * - `exceeds_zip_code_limits_for_period_pay_inside` - **Fiserv (aka First Data) only, code 833, exceeds zip code limits for period - pay inside.**
     *
     * - `expired_card` - **Expired card.**
     *
     * - `expired_lock` - **Fiserv (aka First Data) only, code 301, lock has expired on prepaid card.**
     *
     * - `fraudulent` - **Fraud suspected.**
     *
     * - `file_temporarily_unavailable` - **Fiserv (aka First Data) only, code V51, file temporarily unavailable.**
     *
     * - `file_update_error` - **File update error.**
     *
     * - `file_update_unavailable` - **File update unavailable.**
     *
     * - `format_error` - **Error in transaction format.**
     *
     * - `forward_to_issuer` - **Fiserv (aka First Data) only, codes V76 and M76, forward to issuer.**
     *
     * - `fuel_limit_exceeded` - **Fiserv (aka First Data) only, code 563, over fuel limit.**
     *
     * - `fuel_only` - **Fiserv (aka First Data) only, code 804, fuel only.**
     *
     * - `fuel_price_error` - **Fiserv (aka First Data) only, code 565, fuel price error.**
     *
     * - `full_ssn_required` - **Fiserv (aka First Data) only, code 228, need more ID - request full SSN.**
     *
     * - `function_not_supported` - **Function not supported or function unknown.**
     *
     * - `funds_on_hold` - **Fiserv (aka First Data) only, code 235, outstanding auth - funds on hold.**
     *
     * - `general` - **General use for unspecified code.**
     *
     * - `general_error` - **General use for unspecified error code.**
     *
     * - `generic_decline` - **Generic decline code**
     *
     * - `hard_capture_pickup_card` - **Fiserv (aka First Data) only, code 767, hard capture; pick up.**
     *
     * - `hard_negative_info_on_file` - **Fiserv (aka First Data) only, code 729, hard negative info on file.**
     *
     * - `high_risk` - **Fiserv (aka First Data) only, code 246, declined due to high risk.**
     *
     * - `hold_shipping_for_24h` - **Fiserv (aka First Data) only, code 248, conditional approval - hold shipping for 24 hours.**
     *
     * - `honor_with_authentication` - **Fiserv (aka First Data) only, code 708, honor with authentication.**
     *
     * - `illegal_transaction` - **Illegal transaction.**
     *
     * - `imprint_card` - **Fiserv (aka First Data) only, code 802, imprint card.**
     *
     * - `incorrect_cvv` - **Incorrect CVV value.**
     *
     * - `incorrect_number` - **WorldPay (aka Vantiv or Litle) eComm (aka VAP) only, code 820, The card number submitted for tokenization is invalid.**
     *
     * - `incorrect_pin` - **Incorrect PIN value.**
     *
     * - `incorrect_merchant_location` - **Fiserv (aka First Data) only, code 316, incorrect location. There was a problem with the merchant location.**
     *
     * - `incorrect_transaction_version` - **Fiserv (aka First Data) only, code 326, Incorrect transaction version or format number for POS transactions.**
     *
     * - `inquiry_declined` - **WorldPay (aka Vantiv) Core only, code T4, enrollment inquiry declined.**
     *
     * - `insufficient_data` - **Fiserv (aka First Data) only, code 575, insufficient data.**
     *
     * - `insufficient_funds` - **Not enough funds available to satisfy transaction amount.**
     *
     * - `insufficient_security` - **Fiserv (aka First Data) only, code 573, insufficient security.**
     *
     * - `internet_access_disabled` - **Fiserv (aka First Data) only, code 344, internet access disabled.**
     *
     * - `invalid_3d_password` - **WorldPay (aka Vantiv) Core only, code T1, invalid 3DS password.**
     *
     * - `invalid_account` - **Invalid account.**
     *
     * - `invalid_account/date` - **Fiserv (aka First Data) only, code 518, Invalid account/date or sales date in future.**
     *
     * - `invalid_account_type` - **Fiserv (aka First Data) only, codes 114 and 214, invalid account type or invalid payment type/card type for merchant ID.**
     *
     * - `invalid_activate` - **Invalid activate for gift card.**
     *
     * - `invalid_amount` - **Invalid transaction amount.**
     *
     * - `invalid_authorization_code` - **Invalid authorization code.**
     *
     * - `invalid_batch_number/id` - **Fiserv (aka First Data) only, code 601, invalid batch number/ invalid batch ID or invalid OpenBatch.**
     *
     * - `invalid_cashback` - **Fiserv (aka First Data) only, codes M38 and V38, invalid cashback.**
     *
     * - `invalid_checking_account_number` - **Fiserv (aka First Data) only, code 506, invalid checking account number.**
     *
     * - `invalid_clerk` - **Fiserv (aka First Data) only, code 319, invalid clerk. The clerk field was either missing, when required, or the content did not match the requirements.**
     *
     * - `invalid_currency` - **Invalid transaction currency.**
     *
     * - `invalid_cvv` - **Invalid CVV value.**
     *
     * - `invalid_date_or_time` - **Fiserv (aka First Data) only, codes 345, 555, M67 and V67, invalid date and/or time.**
     *
     * - `invalid_driver` - **Fiserv (aka First Data) only, code 551, invalid driver.**
     *
     * - `invalid_ean_or_scv` - **Fiserv (aka First Data) only, code 300, invalid EAN or SCV.**
     *
     * - `invalid_effective_date` - **Fiserv (aka First Data) only, code 519, invalid effective date.**
     *
     * - `invalid_email_address` - **Fiserv (aka First Data) only, code 227, invalid email address.**
     *
     * - `invalid_entry` - **Fiserv (aka First Data) only, code 157, invalid entry**
     *
     * - `invalid_expiry_date` - **Fiserv (aka First Data) only, codes 132, M14 and V14, unmatched card expiry date.**
     *
     * - `invalid_expiry_year` - **WorldPay (aka Vantiv or Litle) eComm (aka VAP) only, code 320, invalid expiry year.**
     *
     * - `invalid_host` - **Fiserv (aka First Data) only, codes M86 and V86, invalid - host not available.**
     *
     * - `invalid_location` - **Fiserv (aka First Data) only, code 808, invalid location.**
     *
     * - `invalid_maiden_name` - **WorldPay (aka Vantiv) Core only, code T3, invalid mother’s maiden name.**
     *
     * - `invalid_mcc` - **Fiserv (aka First Data) only, code 219, invalid MCC.**
     *
     * - `invalid_merchant` - **Invalid merchant.**
     *
     * - `invalid_merchant_id` - **Invalid merchant ID.**
     *
     * - `invalid_merchant_id/se` - **Fiserv (aka First Data) only, code 527, network response indicates that merchant ID / SE is invalid.**
     *
     * - `invalid_merchant_setup` - **Fiserv (aka First Data) only, code 150, invalid merchant setup.**
     *
     * - `invalid_micr_setup` - **Fiserv (aka First Data) only, code 245, invalid MICR setup.**
     *
     * - `invalid_new_password` - **Fiserv (aka First Data) only, code 321, invalid new password. The new password does not meet the minimum security criteria.**
     *
     * - `invalid_not_present` - **WorldPay (aka Vantiv) Core only, code S, invalid not present for CVV.**
     *
     * - `invalid_number` - **Invalid number.**
     *
     * - `invalid_odometer` - **Fiserv (aka First Data) only, code 560, invalid odometer.**
     *
     * - `invalid_password` - **Fiserv (aka First Data) only, code 320, invalid password.**
     *
     * - `invalid_pin` - **Invalid PIN value.**
     *
     * - `invalid_pos` - **Fiserv (aka First Data) only, codes M13, M93, V12 and V93, invalid POS.**
     *
     * - `invalid_preapproval_number` - **Fiserv (aka First Data) only, code 226, invalid pre-approval number.**
     *
     * - `invalid_prefix` - **Fiserv (aka First Data) only, code M12, invalid prefix.**
     *
     * - `invalid_product` - **Fiserv (aka First Data) only, code 552, invalid product.**
     *
     * - `invalid_prompt_entry` - **Fiserv (aka First Data) only, code 557, invalid prompt entry.**
     *
     * - `invalid_request` - **Fiserv (aka First Data) only, codes 327 and 328, request not permitted by this account or merchant location.**
     *
     * - `invalid_response` - **Fiserv (aka First Data) only, code 944, invalid response.**
     *
     * - `invalid_response_date` - **Fiserv (aka First Data) only, code 233, auth response date not valid.**
     *
     * - `invalid_restriction_code` - **Fiserv (aka First Data) only, code 561, invalid restriction code.**
     *
     * - `invalid_secure_payment_data` - **Fiserv (aka First Data) only, code 217, missing or invalid secure payment data.**
     *
     * - `invalid_sku` - **Fiserv (aka First Data) only, code 211, invalid SKU number.**
     *
     * - `invalid_ssn` - **WorldPay (aka Vantiv) Core only, code T2, invalid SSN.**
     *
     * - `invalid_state_code` - **Fiserv (aka First Data) only, codes 502, M17 and V17, invalid state code.**
     *
     * - `invalid_status_change` - **Unused. Invalid status change.**
     *
     * - `invalid_terminal` - **Fiserv (aka First Data) only, code 130, invalid terminal.**
     *
     * - `invalid_tgn_cic` - **Fiserv (aka First Data) only, code V82, invalid - TGN/CIC not validated.**
     *
     * - `invalid_timezone` - **Fiserv (aka First Data) only, code 369, invalid timezone.**
     *
     * - `invalid_tppid` - **Fiserv (aka First Data) only, code 133, the TPP ID was not found.**
     *
     * - `invalid_track_data` - **Fiserv (aka First Data) only, code 558, invalid track2 data.**
     *
     * - `invalid_transaction` - **Fiserv (aka First Data) only, codes 215, 902, 012, 44 (this should probably really be M44), M15, M19, V15 and V44, invalid transaction.**
     *
     * - `invalid_transarmor_transaction` - **Fiserv (aka First Data) only, code 216, invalid TransArmor transaction.**
     *
     * - `invalid_user` - **Fiserv (aka First Data) only, code 569, invalid user.**
     *
     * - `invalid_value_in_the_field` - **Fiserv (aka First Data) only, code 722, invalid value in the field.**
     *
     * - `invalid_vehicle` - **Fiserv (aka First Data) only, code 550, invalid vehicle.**
     *
     * - `invalid_void_of_activation` - **Fiserv (aka First Data) only, code 342, void of activation after account activity.**
     *
     * - `invalid_zip_code` - **Fiserv (aka First Data) only, code 721, invalid zip code.**
     *
     * - `issuance_under_minimum_amount` - **Fiserv (aka First Data) only, code 234, issuance under minimum amount.**
     *
     * - `issuer_not_available` - **Card issuer not available.**
     *
     * - `issuer_processor_or_switch_not_available` - **Fiserv (aka First Data) only, code 907, card issuer or switch inoperative or processor not available.**
     *
     * - `issuer_special_conditions` - **Fiserv (aka First Data) only, code 108, refer to issuer’s special conditions.**
     *
     * - `issuer_velocity_amount_exceeded` - **Fiserv (aka First Data) only, code 812, over issuer velocity amount.**
     *
     * - `issuer_velocity_count_exceeded` - **Fiserv (aka First Data) only, code 811, over issuer velocity count.**
     *
     * - `late_reversal` - **Late reversal**
     *
     * - `law_violation` - **Fiserv (aka First Data) only, code 124, violation of law.**
     *
     * - `limit_check_failed` - **Fiserv (aka First Data) only, code 924, limit check failed.**
     *
     * - `lost_card` - **Lost card.**
     *
     * - `maiden_name_unavailable` - **WorldPay (aka Vantiv) Core only, code T6, mother’s maiden name unavailable.**
     *
     * - `manual_tries_exceeded` - **WorldPay (aka Vantiv) Core only, code 17, manual PAN tries exceeded.**
     *
     * - `match` - **Verification successful, values provided matched.**
     *
     * - `max_balance_exceeded` - **Fiserv (aka First Data) only, code 317, max balance exceeded. The transaction, if completed, would cause the account balance to be exceeded by the max_balance as specified in the promotion. Some merchants set the max_balance to a value twice the max transaction amount.**
     *
     * - `merchant_depleted` - **Merchant depleted on gift card.**
     *
     * - `merchant_has_mail_pending` - **Fiserv (aka First Data) only, code 576, merchant has mail pending.**
     *
     * - `message_format_error` - **Error in transaction message format.**
     *
     * - `minus2_debit` - **Fiserv (aka First Data) only, code 999, minus 2 debit.**
     *
     * - `missing_conditional_data` - **Fiserv (aka First Data) only, code 212, missing conditional data.**
     *
     * - `missing_customer_service_phone` - **Fiserv (aka First Data) only, code 220, customer service phone number missing.**
     *
     * - `missing_from_account` - **No “from” account specified.**
     *
     * - `missing_to_account` - **No “to” account specified.**
     *
     * - `multi_currency_dcc_fail` - **Multi-currency dynamic currency conversion failure.**
     *
     * - `multi_currency_invert_fail` - **Multi-currency inversion failure.**
     *
     * - `multiple_accounts_for_type` - **WorldPay (aka Vantiv) Core only, code 59, multiple accounts for type**
     *
     * - `name_email_no_match` - **AAVS verification partially successful but name and email provided did not match.**
     *
     * - `name_email_unavailable` - **AAVS verification partially successful but name and email matching was unavailable.**
     *
     * - `name_no_match` - **AAVS verification partially successful but name provided did not match.**
     *
     * - `name_phone_no_match` - **AAVS verification partially successful but name and phone provided did not match.**
     *
     * - `name_phone_unavailable` - **AAVS verification partially successful but name and phone matching was unavailable.**
     *
     * - `name_unavailable` - **AAVS verification partially successful but name matching was unavailable.**
     *
     * - `negative_file_exception` - **WorldPay (aka Vantiv) Core only, code V4, velocity - negative file exception.**
     *
     * - `new_account_information_available` - **Fiserv (aka First Data) only, codes 503 and 507, new account information (available).**
     *
     * - `new_card_issued` - **Fiserv (aka First Data) only, code 237, deny - new card issued.**
     *
     * - `new_password_required` - **Fiserv (aka First Data) only, code 340, new password required.**
     *
     * - `no_account_link` - **Fiserv (aka First Data) only, code 368, no account link.**
     *
     * - `no_action_taken` - **Fiserv (aka First Data) only, code V49, no action taken (can’t backout prior).**
     *
     * - `no_card_record` - **No card record**
     *
     * - `no_chain_id` - **Fiserv (aka First Data) only, code 825, no chain ID for chain merchant.**
     *
     * - `no_checking_account` - **Fiserv (aka First Data) only, codes 539, M55 and V55, no checking account.**
     *
     * - `mismatch` - **Verification unsuccessful, values provided did not match.**
     *
     * - `no_messages_pending` - **Fiserv (aka First Data) only, code 577, no messages pending.**
     *
     * - `no_open_batch` - **Fiserv (aka First Data) only, code 602, no open batch.**
     *
     * - `no_phone_service` - **Fiserv (aka First Data) only, code 343, no phone service. Attempted a calling card transaction on an account which is not configured for calling card activity.**
     *
     * - `no_previous_transaction` - **Fiserv (aka First Data) only, code 333, no previous transaction. The void or reversal transaction could not be matched to a previous (original) transaction. In the case of a redemption, the corresponding locking transaction could not be identified.**
     *
     * - `no_reason_to_decline` - **Fiserv (aka First Data) only, codes 785, M71 and V71, no reason to decline.**
     *
     * - `no_savings_account` - **Fiserv (aka First Data) only, code 541, no savings account.**
     *
     * - `no_such_issuer` - **No such issuer**
     *
     * - `no_to_account` - **Fiserv (aka First Data) only, code 021, no to account.**
     *
     * - `no_transactions_available` - **Fiserv (aka First Data) only, code 338, no transactions available/no more transactions available. There are no transactions for this account or there are no transactions as determined by the specified first transaction number.**
     *
     * - `no_transaction_security_record` - **Fiserv (aka First Data) only, code 574, no transaction security record.**
     *
     * - `not_accepted` - **Fiserv (aka First Data) only, code V91, transaction not accepted.**
     *
     * - `not_active` - **Fiserv (aka First Data) only, code 724, referred - not active.**
     *
     * - `not_lost_or_stolen` - **Fiserv (aka First Data) only, code 311, not lost or stolen.**
     *
     * - `not_on_file` - **Fiserv (aka First Data) only, code 803, not on file.**
     *
     * - `not_permitted` - **Transaction not permitted for cardholder.**
     *
     * - `not_performed` - **WorldPay (aka Vantiv or Litle) eComm (aka VAP) only, code 34, AVS verification not performed.**
     *
     * - `not_processed` - **WorldPay (aka Vantiv) Core only, code P, CVV not processed**
     *
     * - `not_supported` - **Verification not supported.**
     *
     * - `nsf` - **Not sufficient funds available to satisfy transaction amount.**
     *
     * - `one_time_stop_payment` - **Fiserv (aka First Data) only, code 230, one time stop payment requested by cardholder.**
     *
     * - `original_authorization_mismatch` - **Fiserv (aka First Data) only, code 251, re-authorization request is declined. The customer account number, merchant id, or amount did not match the original authorization.**
     *
     * - `original_authorization_not_found` - **Fiserv (aka First Data) only, code 250 and 914, original authorization could not be found.**
     *
     * - `original_repeated` - **Fiserv (aka First Data) only, codes M66 and V66, original repeat - reversal inconsistent.**
     *
     * - `original_transaction_not_approved` - **Fiserv (aka First Data) only, code 244, original transaction not approved.**
     *
     * - `over_chain_daily_velocity_amount` - **Fiserv (aka First Data) only, code 820, over chain daily velocity amount.**
     *
     * - `over_chain_daily_velocity_count` - **Fiserv (aka First Data) only, code 819, over chain daily velocity count.**
     *
     * - `over_chain_product_velocity_limits` - **Fiserv (aka First Data) only, code 821, over chain daily velocity limits.**
     *
     * - `over_chain_product_velocity_amount` - **Fiserv (aka First Data) only, code 824, over chain product velocity amount.**
     *
     * - `over_chain_product_velocity_count` - **Fiserv (aka First Data) only, code 822, over chain product velocity count.**
     *
     * - `over_max_balance` - **Over maximum balance on gift card.**
     *
     * - `over_merchandise_limit` - **Fiserv (aka First Data) only, code 801, over merchandise limit.**
     *
     * - `over_merchant_daily_velocity_amount` - **Fiserv (aka First Data) only, code 814, over merchant daily velocity amount.**
     *
     * - `over_merchant_daily_velocity_count` - **Fiserv (aka First Data) only, code 813, over merchant daily velocity count.**
     *
     * - `over_merchant_daily_velocity_limits`- **Fiserv (aka First Data) only, code 815, over merchant daily velocity limits.**
     *
     * - `over_merchant_product_velocity_amount` - **Fiserv (aka First Data) only, code 816, over merchant product velocity amount.**
     *
     * - `over_merchant_product_velocity_count` - **Fiserv (aka First Data) only, code 817, over merchant product velocity count.**
     *
     * - `over_merchant_product_velocity_limits` - **Fiserv (aka First Data) only, code 818, over merchant product velocity limits.**
     *
     * - `over_monthly_limit` - **Fiserv (aka First Data) only, code 154, over monthly limit.**
     *
     * - `over_non_fuel_limit` - **Fiserv (aka First Data) only, code 807, over non-fuel limit.**
     *
     * - `pan_trans_error` - **Fiserv (aka First Data) only, codes M08 and V08, PAN transaction error.**
     *
     * - `partial_auth_not_allowed` - **Fiserv (aka First Data) only, code 222, partial authorization not allowed.**
     *
     * - `partial_reversal` - **Partial authorization reversal.**
     *
     * - `partially_approved` - **Transaction amount partially approved.**
     *
     * - `password_retries_exceeded` - **Fiserv (aka First Data) only, code 323, password retry exceeded. The user account has been frozen because the user attempted access and was denied. Seek management assistance.**
     *
     * - `pay_at_pump_not_allowed` - **Fiserv (aka First Data) only, code 562, pay at pump not allowed.**
     *
     * - `phone_email_no_match` - **AAVS verification partially successful but phone and email provided did not match.**
     *
     * - `phone_email_unavailable` - **AAVS verification partially successful but phone and email matching was unavailable.**
     *
     * - `phone_no_match` - **AAVS verification partially successful but phone provided did not match.**
     *
     * - `phone_unavailable` - **AAVS verification partially successful but phone matching was unavailable.**
     *
     * - `pickup_card` - **Merchant should hold card and contact issuer (if able).**
     *
     * - `pin_already_exists` - **WorldPay (aka Vantiv) Core only, code T7, PIN already exists in database.**
     *
     * - `pin_key_error` - **PIN key synchronization error.**
     *
     * - `pin_key_sychronization_error` - **Fiserv (aka First Data) only, code 532, pin key synchronization error.**
     *
     * - `pin_try_exceeded` - **Allowed number of PIN tries exceeded.**
     *
     * - `please_retry` - **Fiserv (aka First Data) only, codes 505 and 516, please retry transaction.**
     *
     * - `process_as_debit` - **Credit transaction can be performed as debit.**
     *
     * - `processing_error` - **General processing and/or routing error.**
     *
     * - `product_code_restricted` - **Fiserv (aka First Data) only, code 374, product code(s) restricted.**
     *
     * - `promo_location_restricted` - **Fiserv (aka First Data) only, code 372, promotional location restricted.**
     *
     * - `quantity_exceeded` - **Fiserv (aka First Data) only, code 556, exceeds quantity.**
     *
     * - `reauthorization_timeframe_expired` - **Fiserv (aka First Data) only, code 253, re-authorization request is declined. The timeframes for re-authorization have been exceeded.**
     *
     * - `recharge_amount_exceeded` - **Fiserv (aka First Data) only, code 155, recharge amount exceeded.**
     *
     * - `recharge_count_exceeded` - **Fiserv (aka First Data) only, code 156, recharge count exceeded.**
     *
     * - `reenter_transaction` - **Re-enter transaction and try again.**
     *
     * - `referral` - **Fiserv (aka First Data) only, codes M26, V04, V26 and V39, referral.**
     *
     * - `reject_message` - **Fiserv (aka First Data) only, code 903, reject message.**
     *
     * - `rejected_lost/stolen_checks` - **Fiserv (aka First Data) only, code 731, reject lost or stolen checks.**
     *
     * - `repair_limit_exceeded` - **Fiserv (aka First Data) only, code 567, repair limit exceeded.**
     *
     * - `request_in_progress` - **Fiserv (aka First Data) only, code 923, request in progress.**
     *
     * - `response_received_late` - **Response received late.**
     *
     * - `restricted_card` - **Card is restricted**
     *
     * - `resubmission_violates_network_frequency` - **Fiserv (aka First Data) only, code 529, resubmission of transaction violates debit/EBT network frequency.**
     *
     * - `retain_card` - **Fiserv (aka First Data) only, code 067, retain card.**
     *
     * - `reversal` - **Fiserv (aka First Data) only, codes M28 and V28, reversal.**
     *
     * - `reversal_no_match` - **Reversal does not match original transaction.**
     *
     * - `reversal_unsuccessful` - **Reversal transaction unsuccessful.**
     *
     * - `revocation_of_all_authorizations` - **All authorizations are revoked.**
     *
     * - `revocation_of_authorization` - **Authorization revoked.**
     *
     * - `secure_code_not_enabled` - **Fiserv (aka First Data) only, code 218, merchant ID not enabled for Secure Code.**
     *
     * - `security_error` - **Fiserv (aka First Data) only, code 921, security hardware of software error - no action.**
     *
     * - `security_error_try_again` - **Fiserv (aka First Data) only, code 920, security hardware of software error - try again.**
     *
     * - `security_violation` - **Security violation.**
     *
     * - `see_attendant` - **Fiserv (aka First Data) only, code 792, see attendant.**
     *
     * - `see_exhibit_for_error` - **Fiserv (aka First Data) only, codes M77 and V77, see exhibit for error.**
     *
     * - `service_not_allowed` - **The service requested is not allowed.**
     *
     * - `signature_required` - **Fiserv (aka First Data) only, code 826, signature is required.**
     *
     * - `skip_trace_info` - **Fiserv (aka First Data) only, code 728, referred - skip trace info.**
     *
     * - `soft_descriptor_not_enabled` - **Fiserv (aka First Data) only, code 221, merchant not enabled for soft descriptors.**
     *
     * - `ssn_not_available` - **WorldPay (aka Vantiv) Core only, code T5, SSN not available.**
     *
     * - `stolen_card` - **Card was stolen.**
     *
     * - `stop_payment_order` - **Stop payment order.**
     *
     * - `stop_recurring_payment_requests` - **Fiserv (aka First Data) only, code 791, not approved. Used only in Visa bill/recurring payment. Merchant must stop recurring payment requests.**
     *
     * - `store_location_zero_not_allowed` - **Fiserv (aka First Data) only, code 383, store location zero not allowed.**
     *
     * - `subsystem_unavailable` - **Fiserv (aka First Data) only, code 775, subsystem unavailable.**
     *
     * - `system_error` - **Fiserv (aka First Data) only, codes 906, 909, M20, M35, M75, V20, V25 and V36, system error.**
     *
     * - `target_card_and_transaction_count_mismatch` - **Fiserv (aka First Data) only, code 367, target embossed card entered and Transaction count entered do not match.**
     *
     * - `terminal_transaction_number_mismatch` - **Fiserv (aka First Data) only, code 359, the terminal transaction number did not match (on a void or reversal).**
     *
     * - `testmode_decline` - **Unused. Transaction declined in test mode.**
     *
     * - `ticket_limit_exceeded` - **WorldPay (aka Vantiv) Core only, code 65, daily card ticket limit exceeded.**
     *
     * - `timeout` - **Fiserv (aka First Data) only, codes 008, 093 and V40, timeout error.**
     *
     * - `timeout_reversal_not_supported` - **Fiserv (aka First Data) only, code 915, timeout reversal not supported. Resend the original transaction with the same Reference Number that timed out. Do not retry the timeout reversal.**
     *
     * - `too_many_transactions_requested` - **Fiserv (aka First Data) only, code 337, too many transaction requested.**
     *
     * - `totals_unavailable` - **Fiserv (aka First Data) only, code 740, totals unavailable.**
     *
     * - `transaction_count_or_withdrawal_limit_exceeded` - **Fiserv (aka First Data) only, code 528, debit/EBT transaction count exceeds pre-determined limit in specified time/Withdrawal limit exceeded.**
     *
     * - `transaction_destination_not_found_for_routing` - **Fiserv (aka First Data) only, code 908, transaction destination not found for routing.**
     *
     * - `transaction_history_unavailable` - **Fiserv (aka First Data) only, code 339, transaction history not available.**
     *
     * - `transaction_not_allowed` - **Transaction is not allowed.**
     *
     * - `transaction_not_permitted` - **Fiserv (aka First Data) only, codes 513, M59, M60 and V59, transaction not permitted to acquirer or terminal.**
     *
     * - `transaction_total_limit_per_product_class_exceeded` - **Fiserv (aka First Data) only, code 553, transaction total limit per product class exceeded.**
     *
     * - `transarmor_invalid_token_or_account_number` - **Fiserv (aka First Data) only, code 403, TransArmor invalid token or account number.**
     *
     * - `transarmor_key_error` - **Fiserv (aka First Data) only, code 400, TransArmor key error.**
     *
     * - `transarmor_service_unavailable` - **Fiserv (aka First Data) only, code 402, TransArmor service unavailable.**
     *
     * - `try_again_later` - **Try again later.**
     *
     * - `unable_to_locate_previous_message` - **Fiserv (aka First Data) only, codes M65 and V54, unable to locate previous message.**
     *
     * - `unable_to_locate_record` - **Unable to locate record.**
     *
     * - `unable_to_process` - **Fiserv (aka First Data) only, code 103, unable to process TeleCheck recurring transaction with this payment type (not associated with insufficient or uncollected funds).**
     *
     * - `unable_to_verify_pin` - **Unable to verify PIN.**
     *
     * - `unauthorized` - **Unauthorized.**
     *
     * - `unavailable` - **Unavailable.**
     *
     * - `undefined_card` - **Fiserv (aka First Data) only, code 526, undefined card - Debit/EBT network gateway cannot route card based on Merchant Entitlement.**
     *
     * - `underage_customer` - **Fiserv (aka First Data) only, code 223, customer is under 18 years old.**
     *
     * - `unknown_velocity_error_pay_inside` - **Fiserv (aka First Data) only, code 840, unknown velocity error - pay inside**
     *
     * - `unsuccessful` - **Fiserv (aka First Data) only, code 773, unsuccessful.**
     *
     * - `unsupported_message` - **Fiserv (aka First Data) only, code 905, unsupported message. Transaction was rejected. Call your helpdesk or operations support.**
     *
     * - `validation_error` - **Transaction format validation error.**
     *
     * - `velocity_error_pay_inside` - **Fiserv (aka First Data) only, code 827, velocity exception error - pay inside.**
     *
     * - `verification_transaction` - **Fiserv (aka First Data) only, codes M87 and V87, verification transaction.**
     *
     * - `violation_of_business_arrangement` - **Fiserv (aka First Data) only, code 950, violation of business arrangement.**
     *
     * - `visa_oct/mastercard_moneysend_transactions_blocked` - **Fiserv (aka First Data) only, code 588, all Visa OCT / MasterCard MoneySend transactions are blocked for a rolling 24 hour period, or 7 day period (current and prior 6 days), or 30 day period (current and prior 29 days) for given merchant.**
     *
     * - `void_captured_item` - **Fiserv (aka First Data) only, codes M78 and V78, void of captured transaction.**
     *
     * - `voyager_id_problem` - **Fiserv (aka First Data) only, code 559, Voyager ID problem.**
     *
     * - `y_or_n_required` - **Fiserv (aka First Data) only, code 566, ‘Y' or 'N’ required.**
     *
     * - `wait_2_pm_eastern` - **Fiserv (aka First Data) only, code 570, before 14:00 and can’t cut. Wait until 2:00 PM ET.**
     *
     * - `withdrawal_count_limit_exceeded` - **Exceeds withdrawal frequency limit.**
     *
     * - `withdrawal_limit_exceeded` - **Exceeds withdrawal limit.**
     *
     * - `zip_and_address_not_verified` - **AVS or AAVS verification zip code and address could not be verified.**
     *
     * - `address_match_zip_mismatch` - **AVS or AAVS zip code provided matched but address provided did not match.**
     *
     * - `zip_not_verified` - **AVS or AAVS verification partially successful but zip code could not be verified.**
     *
     * - `3ds_authentication_successful` - **3DS verification successful**
     *
     * - `3ds_authentication_invalid` - **3DS verification unsuccessful, values provided were invalid.**
     *
     * - `3ds_authentication_failed` - **3DS verification unsuccessful due to system failure.**
     *
     * - `3ds_authentication_not_validated` - **3DS verification unsuccessful, values provided could not be validated.**
     *
     * - `3ds_authentication_successful_without_liability_shift` - **3DS verification successful without liability shift.**
     *
     *  </details>
     */
    bankCode?: ButtonclikrApi.TerminalTxnResultsModelBankCode;
}
